import React from 'react';
import { Button, Container, Content, Header, Text } from 'native-base';

export default () => (
  <Container>
    <Header />
    <Content>
      <Button>
        <Text>Click Me!</Text>
      </Button>
    </Content>
  </Container>
);
